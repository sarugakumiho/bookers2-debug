<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved:
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>


<div class='container'>
  <div class='row'>
    
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>Editing Book</h1>
  		<%#= render 'form', book: @book %>
  		<%= form_with model: @book do |f| %>
    	  <div class="form-group">
          <%= f.label :Title %><br />
          <%= f.text_field :title, autofocus: true, id:"inputTitle", class: "form-control book_title" %>
        </div>
        <div class="form-group">
          <%= f.label :Opinion %><br />
          <%= f.text_area :body, autofocus: true, id:"inputOpinion", class: "form-control book_body" %>
        </div>
        <div class="form-group">
          <%= f.submit "Update Book", class: "btn btn-sm btn-success", class: "btn btn-success" %>
        </div>
        <% if @book.present? && @book.user == current_user %>
          <%= link_to "Show", @book %>|
          <%= link_to "Back", books_path, class: "back" %>
        <% end %>
　　  <% end %>
  		<%#= link_to 'Show', @book %>
  		<%#= link_to 'Back', books_path, class: "back" %>
  </div>
</div>